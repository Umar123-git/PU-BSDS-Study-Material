#include <cstdio>
#include "welcome.h"
#include "searching.h"

int linsearch(int ar[], int sz, int val)
{
   printf("%s\n", COPYRIGHT);
   printf("\tlinear searching %d from array of size %d\n", val, sz);
   return 0;
}
int binsearch(int ar[], int sz, int val)
{
   printf("%s\n", COPYRIGHT);
   printf("\tbinary searching %d from array of size %d\n", val, sz);
   return 0;
}
